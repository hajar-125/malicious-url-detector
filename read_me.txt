"Cybersecurity isn't just about firewalls; it's about predictive intelligence. 🚀

I built PhishGuard, a real-time phishing detection engine that catches zero-day attacks using XGBoost and Lexical/WHOIS analysis.

🛠 The Stack:

Pipeline: Python ETL extracting 12+ features from raw URLs.

Model: XGBoost Classifier (95% Recall on unseen data).

Deploy: FastAPI microservice wrapped in Docker.

It detects attacks that standard blocklists miss by analyzing the structure of the URL, not just its reputation. Check out the repo below! 👇"